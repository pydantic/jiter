[project]
name = "jiter-workspace"
version = "0.1.0"
requires-python = ">=3.9"

[dependency-groups]
dev = [
    "maturin",
    "pytest",
    "pytest-pretty",
    "dirty-equals",
]
bench = [
    "orjson",
    "ujson",
]
linting = [
    "ruff",
]
pyodide-build = [
    "pyodide-build",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["crates/jiter-python"]

[tool.ruff]
target-version = "py310"

[tool.ruff.format]
quote-style = "single"
